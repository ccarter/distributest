logger {
  file_appender file{
    dir = "/tmp",
    level = info,
    file = "distributed_runner",
    type = size,
    max = 100000,
    suffix = txt,
    rotation = 2,
    format = '[%L] %I %l%n'
  }
	console_appender cmd{
	  level = warn,
	  format = '[%L] %I %l%n'
	}
}
