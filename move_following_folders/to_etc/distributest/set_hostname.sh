#!/bin/bash
#Replace fully qualified hostname here
#This is used to start the vms with proper hostnames as most boxes do not have them set locally
export DISTRIBUTEST_LOCAL_HOSTNAME=#PUT HOSTNAME HERE!!!!!
